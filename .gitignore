.vs/
bin/
obj/
*.user
firebase-service-account.json
la-plateforme-de-l-emploi-*.json
**/wwwroot/uploads/

# Les CV, sortis de wwwroot pour cesser d'etre servis sans
# authentification. Ils ne doivent pas non plus entrer au depot.
**/donnees/
**/sauvegardes/

# Le journal roulant de Serilog. C'est une sortie d'execution, au meme
# titre que « bin » : elle porte les erreurs reelles des visiteurs, avec
# leurs numeros de reference, et n'a rien a faire au depot.
journaux/
